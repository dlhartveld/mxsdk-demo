# Demo of Mendix Platform API & SDK features

## How to build

Make sure to install tsd globally:

    npm install --global tsd

Install dependencies:

    npm install
    tsd install

Compile & run the script:

    npm start
  